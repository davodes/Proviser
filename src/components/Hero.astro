---
import { ArrowRight, Shield, Users, Trophy } from 'lucide-astro';
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Background -->
  <div class="absolute inset-0">
    <img 
      src="https://images.pexels.com/photos/1578662/pexels-photo-1578662.jpeg?auto=compress&cs=tinysrgb&w=1920&h=1080&fit=crop"
      alt="Construcción profesional"
      class="w-full h-full object-cover"
      loading="eager"
    />
    <div class="absolute inset-0 bg-gradient-to-r from-gray-900/80 via-gray-900/60 to-transparent"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="max-w-4xl">
      <div class="animate-fade-up">
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-white mb-6 leading-tight">
          Construimos el <span class="text-primary-400">futuro</span> con calidad y confianza
        </h1>
        <p class="text-xl md:text-2xl text-gray-200 mb-8 max-w-2xl leading-relaxed">
          Más de 15 años especializados en construcción, rehabilitación y obra civil. 
          Tu proyecto en las mejores manos.
        </p>
      </div>

      <!-- CTAs -->
      <div class="flex flex-col sm:flex-row gap-4 mb-12 animate-fade-up" style="animation-delay: 0.2s">
        <a href="#contacto" class="group inline-flex items-center justify-center px-8 py-4 bg-primary-600 text-white rounded-lg font-semibold hover:bg-primary-700 transition-all duration-300 transform hover:scale-105">
          Solicitar Presupuesto
          <ArrowRight class="w-5 h-5 ml-2 group-hover:translate-x-1 transition-transform" />
        </a>
        <a href="/clientes" class="inline-flex items-center justify-center px-8 py-4 border-2 border-white/30 text-white rounded-lg font-semibold hover:bg-white/10 backdrop-blur-sm transition-all duration-300">
          Ver Proyectos
        </a>
      </div>

      <!-- Stats -->
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 animate-fade-up" style="animation-delay: 0.4s">
        <div class="bg-white/10 backdrop-blur-sm rounded-lg p-6 text-center">
          <Shield class="w-8 h-8 text-primary-400 mx-auto mb-3" />
          <div class="text-2xl font-bold text-white">15+</div>
          <div class="text-gray-300 text-sm">Años de Experiencia</div>
        </div>
        <div class="bg-white/10 backdrop-blur-sm rounded-lg p-6 text-center">
          <Users class="w-8 h-8 text-primary-400 mx-auto mb-3" />
          <div class="text-2xl font-bold text-white">500+</div>
          <div class="text-gray-300 text-sm">Proyectos Completados</div>
        </div>
        <div class="bg-white/10 backdrop-blur-sm rounded-lg p-6 text-center">
          <Trophy class="w-8 h-8 text-primary-400 mx-auto mb-3" />
          <div class="text-2xl font-bold text-white">100%</div>
          <div class="text-gray-300 text-sm">Clientes Satisfechos</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-float">
    <div class="w-6 h-10 border-2 border-white/50 rounded-full flex justify-center">
      <div class="w-1 h-2 bg-white/70 rounded-full mt-2 animate-pulse"></div>
    </div>
  </div>
</section>